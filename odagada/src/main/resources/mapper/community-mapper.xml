<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="community">
  <insert id="saveMessage" parameterType="msgVo">
  		insert into chattingview values(#{receiver},#{sender},sysdate,#{cContent},#{roomId})
  </insert>
  
  <select id="bringMsg" parameterType="string" resultType="map">
  	select * from chattingView join member on (receiver=memberid) where roomId=#{roomId} order by cDate
  </select>
  
  <select id="bringChatRooms" parameterType="string" resultType="map">
  	select roomid,memberid,membername,cdate,ccontent from chattingroom join member on(member2=memberid) join chattingview using(roomid) where cdate in (select max(cdate) from chattingroom join chattingview using(roomid) where member1=#{loginId} group by roomid) and member1=#{loginId}
  </select>
  
</mapper>
